import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.borribo.app',
  appName: 'BorriboMFI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
